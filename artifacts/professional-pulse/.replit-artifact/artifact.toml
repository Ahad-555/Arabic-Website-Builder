kind = "web"
previewPath = "/"
title = "النبض المهني"
version = "1.0.0"
id = "artifacts/professional-pulse"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24942

[services.development]
run = "pnpm --filter @workspace/professional-pulse run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/professional-pulse", "run", "build" ]
publicDir = "artifacts/professional-pulse/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24942"
BASE_PATH = "/"
